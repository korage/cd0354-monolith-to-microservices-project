language: node_js
node_js:
  - 13

services:
  - docker

# Pre-testing installs
install:
  - echo "nothing needs to be installed"

# Scripts to be run such as tests
before_script:
  - echo "no tests"

script:
  - docker --version # print the version for loggin
  - docker image prune -f -a
  - docker-compose -f docker-compose-build.yaml build --parallel
  - docker tag udagram-api-feed docker_hub/udagram-api-feed
  - docker tag udagram-api-user docker_hub/udagram-api-user
  - docker tag udagram-frontend:local docker_hub/udagram-frontend
  - docker tag udagram-reverseproxy:latest docker_hub/udagram-reverseproxy

after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker-compose -f docker-compose-build.yaml push
  
